# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.coverage

# Build output (CMS.exe is rebuilt from source; 45+ MB doesn't belong in git)
build/
dist/
*.spec

# CMS generated memory layer — regenerable via `cms run-all`.
# Delete this line if you want agents to get the memory map on clone.
.memory/

# Per-machine workspace link
cms.workspace.json

# Raw screenshot drops at the repo root (curated copies live in docs/images/)
/Images/

# Per-machine agent permissions; never publish local command allowlists.
/.claude/settings.local.json
